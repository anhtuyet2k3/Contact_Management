<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- USES PERMISSION FOR CAMERA -->
    <!-- USES PERMISSION FOR CAMERA -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:targetApi="30" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        tools:targetApi="30" /> <!-- USES-FEATURE FOR CAMERA -->
    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ContactManagementApp"
        tools:targetApi="31">
        <activity
            android:name=".unit_edit_activity"
            android:exported="false" />
        <activity
            android:name=".employ_edit_activity"
            android:exported="false" />
        <activity
            android:name=".UnitDetailActivity"
            android:exported="false" />
        <activity
            android:name=".EmployeeDetailActivity"
            android:exported="false" />
        <activity
            android:name=".addUnitActivity"
            android:exported="false" />
        <activity
            android:name=".addEmployeeActivity"
            android:exported="false" />
        <activity
            android:name=".HomePageActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>